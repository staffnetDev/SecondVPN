<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@color/primary_color_variant"
    android:layout_marginTop="3dp"
    android:padding="16dp"
    android:visibility="gone"
    android:animateLayoutChanges="true">

    <!-- Custom Configuration Toggle -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="16dp">

        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@android:drawable/ic_menu_preferences"
            android:layout_marginEnd="12dp"
            app:tint="@color/white" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/vmess_enable_custom_config"
            android:textColor="@color/white"
            android:textSize="16sp" />

        <androidx.appcompat.widget.SwitchCompat
            android:id="@+id/vmess_custom_config_switch"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:checked="false" />

    </LinearLayout>

    <!-- Custom Host Input -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/vmess_custom_host_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:hint="@string/vmess_custom_host"
        app:boxStrokeColor="@color/white"
        app:hintTextColor="@color/white"
        app:endIconMode="clear_text"
        app:endIconTint="@color/white"
        app:startIconDrawable="@android:drawable/ic_menu_mylocation"
        app:startIconTint="@color/white">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/vmess_custom_host_input"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/white"
            android:textColorHint="@color/white"
            android:hint="@string/vmess_custom_host_hint"
            android:inputType="textUri"
            android:maxLines="1"
            android:enabled="false" />

    </com.google.android.material.textfield.TextInputLayout>

    <!-- Custom SNI Input -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/vmess_custom_sni_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:hint="@string/sni"
        app:boxStrokeColor="@color/white"
        app:hintTextColor="@color/white"
        app:endIconMode="clear_text"
        app:endIconTint="@color/white"
        app:startIconDrawable="@android:drawable/ic_menu_compass"
        app:startIconTint="@color/white">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/vmess_custom_sni_input"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/white"
            android:textColorHint="@color/white"
            android:hint="@string/vmess_custom_sni_hint"
            android:inputType="textUri"
            android:maxLines="1"
            android:enabled="false" />

    </com.google.android.material.textfield.TextInputLayout>

    <!-- Save Configuration Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/vmess_save_config_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/vmess_save_config"
        android:textColor="@color/black"
        android:textStyle="bold"
        app:backgroundTint="@color/white"
        app:cornerRadius="8dp"
        app:icon="@android:drawable/ic_menu_save"
        app:iconTint="@color/black"
        app:iconGravity="textStart"
        app:strokeColor="@color/white"
        app:strokeWidth="2dp"
        android:enabled="false" />

</LinearLayout>